import 'package:flutter/material.dart';

class ComplianceTracker extends StatelessWidget {
  const ComplianceTracker({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Compliance Tracker"),
      ),
    );
  }
}
