import 'package:flutter/material.dart';

class DecisionLog extends StatelessWidget {
  const DecisionLog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Decision Log"),
      ),
    );
  }
}
