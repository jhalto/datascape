import 'package:flutter/material.dart';

class SubmissionTracker extends StatelessWidget {
  const SubmissionTracker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Submission Tracker"),
      ),
    );
  }
}
