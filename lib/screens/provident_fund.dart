import 'package:flutter/material.dart';

class ProvidentFund extends StatelessWidget {
  const ProvidentFund({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       backgroundColor: Colors.green,
      appBar: AppBar(
         title: Text("Provident Fund"),
        centerTitle: true,
      ),
    );
  }
}
